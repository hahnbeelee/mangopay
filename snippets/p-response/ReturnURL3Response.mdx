import MaxLength255Characters from '/snippets/rules/max-length-255-characters.mdx';
  
<ResponseField name="ReturnURL" type="string" >  
<MaxLength255Characters />    
The URL to which the user is returned after the hosted identity verification session, whatever the outcome.
</ResponseField>