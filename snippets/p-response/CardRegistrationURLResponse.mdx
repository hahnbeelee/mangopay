<ResponseField name="CardRegistrationURL" type="string" >    
The URL to make the card tokenization call.  
Best practice: Rely on the URL returned rather than directly hard-coding the URL for a more sustainable implementation.  
</ResponseField>