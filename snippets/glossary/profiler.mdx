export const Profiler = ({content}) => (
    <Tooltip tip="Code implemented into the platform’s app or website which monitors browser and behavioral data for fraud prevention purposes.">
        {content}
    </Tooltip>
);