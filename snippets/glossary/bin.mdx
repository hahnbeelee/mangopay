export const Bin = ({content}) => (
    <Tooltip tip="Unique set of digits assigned to an issuing bank, which can give information about an issuer and the card issued.">
        {content}
    </Tooltip>
);