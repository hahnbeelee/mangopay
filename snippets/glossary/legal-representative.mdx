export const LegalRepresentative = ({content}) => (
    <Tooltip tip="An individual legally appointed to represent a legal entity, authorized to sign contracts on its behalf.">
        {content}
    </Tooltip>
);