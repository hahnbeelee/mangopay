export const User = ({content}) => (
    <Tooltip tip="A private individual (natural person) or legal entity (legal person) registered with Mangopay to make payments on a platform.">
        {content}
    </Tooltip>
);