export const MangopayHub = ({content}) => (
    <Tooltip tip="Mangopay interface to centralize the platform’s entire experience with Mangopay, including access to API testing and the Dashboard.">
        {content}
    </Tooltip>
);