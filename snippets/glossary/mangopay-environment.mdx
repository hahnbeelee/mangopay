export const MangopayEnvironment = ({content}) => (
    <Tooltip tip="Secure digital space where funds can be transferred freely between wallets, allowing for flexible payment workflows.">
        {content}
    </Tooltip>
);