export const BankWire = ({content}) => (
    <Tooltip tip="Movement of funds between a user’s bank account and their e-wallet, either entering the Mangopay environment as a pay-in or leaving as a payout.">
        {content}
    </Tooltip>
);