export const BeneficialOwner = ({content}) => (
    <Tooltip tip="An individual who owns or holds a power of control over a legal entity, either directly or via a holding company, usually by owning more than 25% of the entity's share capital or voting rights.">
        {content}
    </Tooltip>
);