export const MangopayDashboard = ({content}) => (
    <Tooltip tip="Mangopay interface enabling platform operators to manage their payment operations, available through the Mangopay Hub.">
        {content}
    </Tooltip>
);