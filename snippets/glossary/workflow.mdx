export const Workflow = ({content}) => (
    <Tooltip tip="Diagram representing flows of funds in the Mangopay environment, allowing a platform to adapt payment operations to its business model and plan the technical integration.">
        {content}
    </Tooltip>
);