  
```php PHP  
// SDK init. To do once.
$api = new \MangoPay\MangoPayApi(); 
$api->Config->ClientId = 'your-client-id';
$api->Config->BaseUrl = 'https://api.sandbox.mangopay.com';
$api->Config->ClientPassword = 'your-api-key';

$api->Responses->get($idempotencyKey);  
```