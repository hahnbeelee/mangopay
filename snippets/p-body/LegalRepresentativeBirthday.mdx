  
<ParamField body="LegalRepresentativeBirthday" type="timestamp" >     
Required if `UserCategory` is `OWNER`. Returned `null` if `UserCategory` is `PAYER`.  

The date of birth of the entity’s legal representative.
</ParamField>