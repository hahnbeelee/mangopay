import _6Or8Digits from '/snippets/rules/6-or-8-digits.mdx';
  
<ParamField body="Bin" type="string" >  
<_6Or8Digits />     
Required if the `Type` is `BIN`.

The bank identification number (BIN). 
</ParamField>